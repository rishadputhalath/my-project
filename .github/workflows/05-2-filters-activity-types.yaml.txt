name: "05-2-Event Filters and Activity Types"
on: [pull-request]

jobs:
 job1:
    - name: "echo to the work flow"
      runs-on: "ubuntu-latest"
       steps:
         run: echo "runs whenever a pr is closed"
